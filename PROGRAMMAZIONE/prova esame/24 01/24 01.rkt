;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-intermediate-lambda-reader.ss" "lang")((modname |24 01|) (read-case-sensitive #t) (teachpacks ((lib "hanoi.ss" "installed-teachpacks") (lib "drawings.ss" "installed-teachpacks"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "hanoi.ss" "installed-teachpacks") (lib "drawings.ss" "installed-teachpacks")) #f)))
(define sort   ;ordina gli elementi della lista in ordine crescente di lunghezza e valore decimale corrispondente
  (lambda (lista)
    ;;APPLICARE LA FUNZIONE sorted-list degli es di laboratorio
    (if (> (string-length (car lista)) ())
    )
  )
)


(define sorted-ins
  (lambda (stringa lista)
       
  )
)